# Version-agnostic: minimum-only pins so the app installs against whatever
# compatible versions the environment already has.
streamlit>=1.56.0
Authlib>=1.3.2
httpx>=0.27.0
pandas>=2.0.0
numpy>=1.24.0
xgboost>=2.0.0
lightgbm>=4.0.0
optuna>=3.4.0
scikit-learn>=1.3.0
breeze-connect>=1.0.55
plotly>=5.17.0
joblib>=1.3.0
python-dateutil>=2.8.2
pytz>=2023.3
requests>=2.31.0
feedparser>=6.0.0

# News sentiment — FinBERT (primary) requires transformers + torch.
# Commented out on the `deployment` branch: Streamlit Community Cloud's free
# tier (1 CPU / 1GB RAM) can't reliably hold torch+transformers alongside
# xgboost/lightgbm/optuna. news_sentiment.py falls back to VADER automatically
# when these aren't importable — no code change needed. Uncomment if you
# upgrade to a bigger Cloud plan or self-host.
# transformers>=4.40.0
# torch>=2.0.0

# VADER (lightweight fallback — used as primary sentiment backend on Cloud)
vaderSentiment>=3.3.2

# Optional: hands-free auto-refresh for Live Monitor
streamlit-autorefresh>=1.0.1

# Groww API (optional — Order Flow Imbalance feature)
pyotp>=2.9.0
growwapi>=0.1.0  # optional — for Order Flow Imbalance
